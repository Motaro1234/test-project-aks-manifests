# templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app-chart.fullname" . }}
  labels:
    {{- include "app-chart.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "app-chart.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app-chart.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "app-chart.serviceAccountName" . }}
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - secretRef:
                name: {{ .Values.database.secretName }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
          env:
            - name: NODE_ENV
              value: {{ .Values.env.NODE_ENV | quote }}
            - name: IMAGES_DIR                          # ← เพิ่มบรรทัดนี้ ในลิสต์ env เดิม
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:                                 # ← เพิ่มใหม่ อยู่ระดับเดียวกับ ports/env/resources
            - name: sharecenter2-volume
              mountPath: /mnt/images
      volumes:                                           # ← เพิ่มใหม่ อยู่ระดับเดียวกับ containers/serviceAccountName
        - name: sharecenter2-volume
          persistentVolumeClaim:
            claimName: sharecenter2-nfs-pvc